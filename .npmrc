# Ensure devDependencies (build tools like @tailwindcss/postcss) always install.
# Overrides any global omit=dev setting that was causing build-tool packages
# to be silently skipped on clean installs.
omit=
include=dev
